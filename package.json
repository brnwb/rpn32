{
  "name": "rpn32-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "A terminal RPN calculator inspired by the HP 32SII",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @rpn32/core && npm run build -w rpn32",
    "dev": "npm run build -w @rpn32/core && npm run dev -w rpn32",
    "test": "vitest --run",
    "check": "npm run build && npm run test"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  },
  "license": "MIT"
}
