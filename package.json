{
  "name": "rpn32-monorepo",
  "version": "0.4.0",
  "private": true,
  "description": "A terminal RPN calculator inspired by the HP 32SII",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "pnpm --filter @brnwb/rpn32-core run clean && pnpm --filter @brnwb/rpn32-cli run clean",
    "build": "pnpm --filter @brnwb/rpn32-core run build && pnpm --filter @brnwb/rpn32-cli run build",
    "dev": "pnpm --filter @brnwb/rpn32-core run build && pnpm --filter @brnwb/rpn32-cli run dev",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "test": "vitest --run",
    "check": "pnpm run format:check && pnpm run lint && pnpm run build && pnpm run test"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0",
    "tsx": "^4.21.0",
    "typescript": "^7.0.2",
    "vitest": "^4.0.14"
  },
  "packageManager": "pnpm@11.2.2"
}
