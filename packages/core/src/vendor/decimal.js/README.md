# decimal.js

This directory vendors `decimal.js` v10.6.0 from <https://github.com/MikeMcl/decimal.js>.

The vendored `decimal.mjs` and `decimal.d.mts` files are copied from the upstream package without local modifications. `LICENCE.md` contains the upstream MIT license and copyright notice.
