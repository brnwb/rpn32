# @rpn32/core

Reusable calculator engine for `rpn32`, a terminal RPN calculator inspired by the HP 32SII.

This package contains the calculator state, command processing, decimal math, display formatting, angle modes, and HP-style four-level stack behavior. It has no Node terminal dependencies, so it can be used by a CLI, TUI, Electron app, web app, or tests.
